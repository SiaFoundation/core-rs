[package]
name = "sia_storage"
version = "0.12.0"
edition = "2024"
repository = "https://github.com/SiaFoundation/sia-sdk-rs"
license = "MIT"
description = "SDK for interacting with a Sia network indexer"
authors = ["The Sia Foundation"]
categories = ["cryptography::cryptocurrencies"]
keywords = ["sia", "decentralized", "blockchain", "depin", "storage"]

[lib]
path = "src/lib.rs"
bench = false

# --- Cross-platform dependencies ---
[dependencies]
serde = "1.0.229"
serde_json = "1.0.151"
thiserror = "2.0.19"
sia_core = { path = "../sia_core", version = "0.5.0" }
rand = "0.10.2"
blake2b_simd = "1.0.4"
url = { version = "2.5.8", features = ["serde"] }
log = "0.4.33"
bytes = "1.12.1"
serde_with = { version = "3.21.0", features = ["base64", "hex"] }
hex = "0.4.3"
chacha20poly1305 = "0.11.0"
chacha20 = { version = "0.10.1", features = ["xchacha"] }
chrono = { version = "0.4.45", features = ["serde"] }
zeroize = { version = "1.9.0", features = ["derive"] }
base64 = "0.23.0"
ed25519-dalek = "3.0.0"
web-time = "1"
getrandom = "0.4"
tokio-util = { version = "0.7.19", features = ["rt"] }
sia_reed_solomon = "0.3.0"

# --- Native-only dependencies ---
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
# do not update to reqwest v0.13 until https://github.com/seanmonstar/reqwest/issues/2968 is resolved
reqwest = { version = "0.12.28", default-features = false, features = ["charset", "http2", "json", "rustls-tls"] }
tokio = { version = "1.53.1", features = ["sync", "io-util", "macros", "rt", "time"] }
sia_mux = { version = "0.1.1", path = "../sia_mux" }
sysinfo = { version = "0.39", default-features = false, features = ["system"] }

# --- WASM-only dependencies ---
[target.'cfg(target_arch = "wasm32")'.dependencies]
reqwest = { version = "0.12.28", default-features = false, features = ["json"] }
tokio = { version = "1.53.1", features = ["sync", "io-util", "macros", "rt"] }
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = { version = "0.3", features = [
    "WebTransport",
    "WebTransportOptions",
    "WebTransportBidirectionalStream",
    "WebTransportSendStream",
    "WebTransportReceiveStream",
    "WebTransportHash",
    "WebTransportCloseInfo",
    "ReadableStream",
    "ReadableStreamDefaultReader",
    "WritableStream",
    "WritableStreamDefaultWriter",
    "TransformStream",
] }
getrandom = { version = "0.4", features = ["wasm_js"] }

[dev-dependencies]
sia_core_derive = { path = "../sia_core_derive", version = "0.1.2" }

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
criterion = { version = "0.8.2", features = ["async_tokio"] }
httptest = "0.16.4"
tokio = { version = "1.53.1", features = ["fs", "macros", "rt-multi-thread", "test-util"] }
env_logger = "0.11.11"
clap = { version = "4.6.4", features = ["derive"] }
indicatif = "0.18.6"
directories = "6.0.0"
toml = "1.1.3"
tempfile = "3.27.0"

[target.'cfg(target_arch = "wasm32")'.dev-dependencies]
wasm-bindgen-test = "0.3"

[features]
mock = []
less-safe-crypto = []
# Adds `Sdk::upload_path`, `PackedUpload::add_path`, and
# `Download::write_to_path`. Not usable on wasm32: tokio rejects its `fs`
# feature there.
fs = ["tokio/fs"]

[[bench]]
name = "upload"
harness = false
required-features = ["mock"]
