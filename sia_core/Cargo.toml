[package]
name = "sia_core"
version = "0.4.2"
edition = "2024"
repository = "https://github.com/SiaFoundation/sia-sdk-rs"
license = "MIT"
description = "Low-level SDK for interacting with the Sia decentralized storage network"
authors = ["The Sia Foundation"]
categories = ["cryptography::cryptocurrencies"]
keywords = ["sia", "decentralized", "blockchain", "depin", "storage"]

[lib]
path = "src/lib.rs"
bench = false

[dependencies]
base64 = "0.23.0"
bip39 = "2.2.2"
blake2b_simd = "1.0.4"
bytes = "1.12.1"
chrono = { version = "0.4.45", features = ["serde"] }
ed25519-dalek = "3.0.0"
hex = "0.4.3"
num-bigint = "0.4.8"
num-rational = { version = "0.4.2", features = ["num-bigint"] }
num-traits = "0.2.19"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sia_core_derive = { version = "0.1.2", path = "../sia_core_derive" }
thiserror = "2.0.19"
uint = "0.10.0"
zeroize = { version = "1.9.0", features = ["zeroize_derive"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tokio = { version = "1.53.1", features = ["macros", "rt", "sync", "io-util", "time"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
tokio = { version = "1.53.1", features = ["macros", "rt", "sync", "io-util"] }

[dev-dependencies]
rand = "0.10.2"

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
criterion = { version = "0.8", features = ["async_tokio"] }

[target.'cfg(target_arch = "wasm32")'.dev-dependencies]
getrandom = { version = "0.4", features = ["wasm_js"] }
wasm-bindgen-test = "0.3"

[[bench]]
name = "merkle_root"
harness = false
